# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: AndyVictory's homepage
email: example@example.com
description: >- # this means to ignore newlines until "baseurl:"
  Log some notes about coding.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://andyvictory.dpdns.org" # the base hostname & protocol for your site, e.g. http://example.com
# twitter_username: vampire
github_username:  vampire

# Language setting
lang: zh-CN

# Defaults (全局默认配置)
defaults:
  - scope:
      path: "_posts"
      type: posts
    values:
      comments: true

# Comments
comments:
  provider: giscus
  giscus:
    repo: vampire007/vampire007.github.io
    repo_id: 'R_kgDOReCzMQ'
    category: Q&A
    category_id: 'DIC_kwDOReCzMc4DC6Np'
    mapping: pathname
    strict: 0
    reactions_enabled: 1
    emit_metadata: 0
    input_position: bottom
    lang: zh-CN

# Avatar (头像照片)
avatar: /assets/img/avatar.jpg

# Build settings
theme: jekyll-theme-chirpy
plugins:
  - jekyll-feed
  - jekyll-paginate
  - jekyll-sitemap
  - jekyll-archives
  - jekyll-include-cache

# Chirpy-specific settings
collections:
  tabs:
    output: true
    sort_by: order
  docs:
    output: true
    permalink: /docs/:path/

# Search configuration
search:
  enabled: true

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/

incremental: false
highlighter: rouge
gist:
  noscript: false
kramdown:
  math_engine: mathjax
  syntax_highlighter: rouge